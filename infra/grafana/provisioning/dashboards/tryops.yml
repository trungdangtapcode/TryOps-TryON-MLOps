apiVersion: 1

providers:
  - name: TryOps Enterprise Dashboards
    orgId: 1
    folder: TryOps
    folderUid: tryops
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: false
    options:
      path: /var/lib/grafana/dashboards
