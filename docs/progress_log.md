# Progress Log

## 2026-06-11

- Created TryOps scaffold from roadmap.
- Added Python policy gate, contracts, lineage, cards, data validation, VTON metric summary, and LLM benchmark summary.
- Added local promotion pipeline that writes decision, data validation, lineage, registry entry, model card, and data card artifacts.
- Added Docker Compose for MLflow, MinIO, PostgreSQL, Prometheus, Grafana, and API.
- Added native C++ policy engine and tests.
- Added Rust gateway scaffold and Go controller scaffold.
- Added research summary, project charter, SLOs, dashboard design, rollback design, and architecture review.
- Updated roadmap checklist to reflect evidence-backed completion.
- Added C4 context/container diagrams, VTON/LLM model ADRs, dataset inventory, and ingestion/checksum/image validation utilities.
- Added VTON preflight layer with image metadata parsing, safety checks, checksums, deterministic cache keys, and tests.
- Added fixed VTON benchmark-set contract, human evaluation rubric, and failure taxonomy.
- Added lightweight PNG utilities, deterministic naive VTON baseline, synthetic demo generator, API adapter route, latency/output lineage, and structural image metrics.
- Added VTON baseline comparison pipeline, error-gallery artifact, exploration notebook, and VTON results section.
- Selected `HuggingFaceTB/SmolLM2-135M-Instruct` as the first small open-source LLM target.
- Added deterministic local LLM baseline, structured output, safety flags, quality scoring, benchmark report, API route, scripts, and tests.
- Added `/v1` API versioning rules, readiness surface, structured request validation, request ID propagation, safe alias routing, canary routing simulation, LLM shadow evaluation, Prometheus-style metrics, sanitized telemetry, and tests.
- Added run context metadata, deployment package generation, release notes, rollback plan/record, deployment profiles, LLM load test, experiment log, reproducibility checklist, and tests.
- Added C++ native policy CLI and Python bridge so promotion runs can emit native policy evidence and compare it with the Python policy gate.
- Added C++ native image metrics CLI, Python bridge, dHash similarity, edge-delta proxy, VTON stage latency, and native metric evidence in VTON comparison artifacts.
- Added optional VTON segmentation/mask and pose preprocessing with a dependency-free C++ native CLI, Python bridge, mask artifacts, pose hints, and baseline sidecar integration.
- Added garment-preservation similarity artifacts with an always-on deterministic patch proxy and an explicit OpenCLIP execution hook for future neural scoring.
- Added LLM prompt-length and output-length sensitivity benchmark artifacts for the local baseline.
- Added usage-based quota checks, hashed user usage telemetry, API quota rejection responses, and optimized LLM fallback-to-baseline routing.
- Added timeout handling, local async VTON job submission/status endpoints, queue-depth metrics, and async job smoke evidence.
- Added structured JSONL API logs, local latency/quality alert threshold evaluation, and Prometheus alert-rule artifacts.
- Added provisioned Grafana dashboards for service health, model quality, and cost/capacity telemetry with local validation evidence.
- Added local drift reports for VTON image metadata and LLM prompt length/topic distributions.
- Added endpoint smoke evidence for `/v1/ready`, `/v1/llm/generate`, `/v1/vton/infer`, and `/v1/metrics`.
- Added NIST AI RMF, OWASP LLM 2025, VTON bias/representation, and residual-risk governance mapping evidence.
- Added least-privilege API-key simulation for promotion and lineage admin actions with redacted auth evidence.
- Added dependency lockfile generation, local SPDX SBOM fallback, model-source pins, and dataset license/usage inventories.
- Added a Kubeflow-target orchestration skeleton that validates the enterprise DAG and emits local manifest/report artifacts.
- Added an LLM optimization report generator that turns the real quantization Pareto artifact into Markdown, CSV, SVG chart, and audit evidence.
- Added LLM prefill/decode phase timing to generation metrics, benchmark artifacts, structured logs, and Prometheus metrics.
- Added OpenTelemetry-compatible trace/span IDs, `traceparent` propagation, sanitized JSONL span artifacts, structured-log correlation, and Prometheus trace metrics.
- Added formal workload SLO error budgets plus a native C++ multi-window burn-rate engine, Prometheus burn-rate rules, and a regression-drill artifact.
- Added runtime LLM guardrails with a native Go HTTP sidecar/CLI, PII redaction, prompt/output safety classification, structured-output gating, OWASP LLM 2025 guardrail reports, Grafana guardrail panels, and promotion-gate integration.
- Added a native C++ model artifact scanner enforcing SafeTensors-only promotion, rejecting pickle-family `.bin` weights, and feeding Python, C++, and Rego promotion gates.
- Added FinOps unit economics, tenant budget showback, budget alert rules, a privacy-aware LLM semantic cache, native C++ semantic-cache lookup, and cache hit/savings panels in the cost dashboard.
- Added a native C++ chaos scenario evaluator for GPU OOM, slow decode, corrupted weights, and poisoned candidates, with native burn-rate evaluation and automatic rollback records.
- Fixed the Go controller smoke path for Go 1.19-compatible routing, made HTTP status failures fail the target, and refreshed the Rust gateway artifact benchmark against Python FastAPI.
- Added local model provenance/signature evidence with an in-toto/SLSA statement, DSSE-shaped bundle, native C++ verifier, and Python/C++/Rego promotion gates requiring provenance before model promotion.
- Added OpenLineage-standard RunEvent emission beside the internal lineage record, plus a native C++ validator whose verdict is carried into deployment evidence.
- Added Argo CD / Argo Rollouts GitOps deployment manifests plus a native C++ validator whose verdict is embedded in deployment packages.
- Added a signed MLflow-style registry webhook deploy trigger in the Go controller, with HMAC freshness/signature validation and GitOps/canary action evidence.
- Added a signed GitHub-style promotion PR trigger in the Go controller, with HMAC webhook validation and merged-PR approval/check/provenance evidence before alias sync.
- Added a native C++ online experiment router with guarded A/B allocation, holdback, UCB-style bandit scoring, routing-layer integration, and smoke evidence showing the unsafe candidate blocked while the challenger receives the bandit route.
- Added a native C++ online experiment statistics engine for holdback uplift confidence intervals and Wald-style sequential early stopping, with Theme-N native bootstrap CI evidence in the analysis report.
- Moved usage-quota admission into the Rust Axum gateway with `/v1/quota/check`, `/v1/quota/snapshot`, a batch CLI, Python API delegation through `TRYOPS_QUOTA_GATEWAY_URL`, and Rust-backed quota sample evidence.
- Added a native C++ VTON advanced evaluation engine for face-region identity embedding proxies, masked garment fidelity, pose consistency, fairness slice gaps, and Bradley-Terry preference ranking, with model-card bias/limitation updates.
- Added a native C++ LLM batch scheduler benchmark comparing static request-level batching with iteration-level continuous batching over a mixed concurrent request stream.
- Added energy/CO2e and cost-vs-energy correlation panels to the cost/capacity Grafana dashboard, with dashboard validation coverage.
- Promoted the Rust gateway from scaffold to product edge: `/api/*` reverse proxy to FastAPI `/v1/*`, generated/forwarded request IDs, native edge rate limits, payload limits, signed-artifact admin preflight, container healthcheck mode, compose gateway service, and `make app-up` / `make app-down`.
- Added native Rust gateway Prometheus metrics, Prometheus scrape wiring for `gateway:8081`, and Grafana service panels for gateway request rate, p95 latency, rate-limit rejects, and upstream errors.
- Fixed FastAPI product backend route registration so history, request detail, feedback, dashboard, models, model promotion, and lineage endpoints are live before `create_app()` returns; added isolated DB/auth regression coverage.
- Added Rust gateway edge enforcement for LLM guardrails via the native Go sidecar, plus a smoke target proving prompt-injection/system-prompt leakage is blocked before proxying to Python.
- Added optional native Rust quota ledger persistence with `TRYOPS_GATEWAY_QUOTA_LEDGER_PATH`, a `tryops.quota_ledger_file.v1` artifact, and `make native-quota-ledger-smoke` proving usage survives separate CLI process runs.
- Extended the Rust quota gateway with split durable and snapshot modules: accepted usage can mirror to Postgres `tryops_quota_usage` upserts plus Valkey-compatible RESP counters, and native snapshots now include hashed tenant aggregates for the BFF/dashboard read model.
- Added a native Go config-contract checker that parses `docker-compose.yml`, validates service envs, healthchecks, readiness dependencies, port variables, named volumes, and Rust gateway env references, then emits `artifacts/eval/config/native_config_contract_report.json`.
- Added Rust gateway `trace_context.rs`, W3C `traceparent` propagation through `/api/*`, trace response headers, and native smoke coverage for trace continuity.
- Split the Go controller into server, handler, signature, promotion, field-helper, and contract modules while preserving signed PR/registry webhook behavior.
- Split the Go guardrail sidecar into server, CLI, evaluator, metrics, and contract modules while preserving HTTP, CLI, Prometheus, and edge-enforcement behavior.
- Split the C++ semantic-cache hot path into reusable core header/source, thin CLI adapter, and native regression test, then wired the Makefile to build and test the module set.
- Added a dependency-free Go native benchmark driver that starts FastAPI plus the Rust gateway, removes the Python/GIL load-driver limitation, benchmarks `/health`, direct validated promotion POST, and full edge proxy promotion POST, and emits `tryops.native_gateway_benchmark.v1`.
- Added the first React/Vite/TypeScript TryOps Console shell with typed API client, live LLM Playground, VTON Studio contract form, dashboard, history, registry, governance, incident posture, API-key session field, degraded banner, and responsive enterprise styling.
- Added native Rust static SPA serving to the gateway with safe path resolution, content types, cache headers, SPA fallback, Docker compose wiring, and `make native-static-smoke` proving `/`, deep-link fallback, and `/api/health` coexist behind the same edge binary.
- Added a native Go full-stack smoke checker and `make app-smoke`, made Compose host ports overrideable for reliable local startup, fixed MLflow startup with PostgreSQL/S3 dependencies, and verified Console, gateway, API, guardrail, Prometheus, Grafana, MinIO, and MLflow readiness in `tryops.full_stack_smoke.v1`.
- Added a native Go professor-demo acceptance checker and `make professor-demo-acceptance`, which runs the bad-candidate policy gate live and validates Pareto, energy, full-stack, VTON, lineage, promotion, rollback, and governance evidence in `tryops.professor_demo_acceptance.v1`.
- Added a native Go vulnerability scan runner and `make vulnerability-scan-sample`, which runs installed scanner coverage (`npm audit` for `web/` here) and records missing production scanners in `tryops.vulnerability_scan.v1`.
- Added a native Go evaluation-artifact indexer and Console Evaluation view: `make evaluation-index-sample` emits `tryops.evaluation_index.v1`, `/api/evaluations/summary` serves it, the React Console renders evidence highlights and a report registry, and `make app-smoke` now proves the endpoint through the Rust gateway.
- Added persisted VTON comparison viewing in the Console: `/api/vton/comparison` enriches `tryops.vton_comparison.v1` with artifact URLs, `/api/artifacts/file` serves allowed PNG/JSON artifacts, the Rust gateway preflights artifact paths before proxying, VTON runtime outputs write under `artifacts/runtime/`, and `make app-smoke` proves comparison JSON plus PNG serving through the gateway.
- Added the live "Block bad model" Incident drill in the Console: a seeded failing VTON candidate runs through `/api/promotion/evaluate` behind the Rust gateway signed-artifact preflight, and the native Go full-stack smoke checker now verifies `bad_candidate_gate_through_gateway` returns `approved=false` with risk-reviewer rejection evidence.
- Added the Console rollback drill: deployment rollback JSON artifacts are safely exposed through the API artifact resolver and Rust gateway preflight, the Incident view loads `rollback_state.json` with a seeded viewer key, and the native Go full-stack smoke checker verifies `rollback_state_artifact_through_gateway`.
- Added the Pipeline Runs Console view: the native Go evaluation indexer aggregates run context, OpenLineage RunEvent, and lineage artifacts into `pipeline_runs`; `/api/evaluations/summary` serves the ledger; the Runs page renders run/job/model/dataset/trace evidence; and `make app-smoke` verifies the ledger through the Rust gateway.
- Added the Console optimization panel: the native Go evaluation indexer combines LLM Pareto, leaderboard, and energy artifacts into `optimization_panel`; the Evaluation view renders an interactive quality-vs-latency frontier, recommendation, per-variant VRAM/energy/SCI, and carbon-gate verdict; and `make app-smoke` verifies the payload through the Rust gateway.
- Added Rust gateway auth preflight for protected Console/API routes: the gateway loads the hashed API-key registry, supports scoped API keys plus optional HS256 bearer JWTs, forwards non-secret principal metadata, exports auth-decision metrics, and `make app-smoke` verifies missing-key 401 plus missing-scope 403 before FastAPI.
- Added a native Go VTON/LLM job runner split across focused modules, using context deadlines, retry backoff, async VTON polling, compact response summaries, and native sample PNG generation; `make native-job-runner-sample` writes `tryops.native_job_runner.v1`, and `make app-smoke` now runs it through the Rust gateway.
- Added a native Go SLO regression gate split across config/load/policy/evaluation/report modules; it consumes `tryops.native_gateway_benchmark.v1`, fails on latency/error/throughput regressions, emits `tryops.native_slo_gate.v1`, and is indexed by the Console evidence registry.
- Added a native Go audit/webhook event dispatcher for promotion, feedback, incident, and quota events; it writes `tryops.native_audit_event.v1` JSONL audit records, signs webhook payloads with HMAC-SHA256, retries delivery, and emits `tryops.native_event_dispatcher.v1` governance evidence.
- Added the Console Professor Demo mode: a seeded seven-step offline walkthrough for stack preflight, native quota admission, bad-model blocking, LLM optimization, VTON quality, promotion lineage, rollback, and governance; the native Go demo acceptance harness now validates quota evidence and the demo source contract.
- Added a native Go professor-demo video recorder: `make professor-demo-video` renders a 9-frame offline walkthrough from the shared Console storyboard JSON, encodes an H.264 MP4 with FFmpeg, and writes `tryops.professor_demo_video.v1` evidence into the evaluation registry.
- Wired the C++ promotion policy engine into the Go controller runtime: signed registry webhooks now carry the full `policy_candidate`, `TRYOPS_CONTROLLER_POLICY_CLI` makes the Go service execute `tryops_policy_cli`, and `make registry-webhook-sample` proves the native verdict is required before GitOps/canary actions are accepted.
- Verified DVC-backed data/artifact versioning against MinIO: `dvc repro` generated `dvc.lock`, `dvc push` uploaded 12 cache objects, and the native Go `tryops_data_versioning` verifier signs S3 requests directly to MinIO and emits `tryops.dvc_minio_versioning.v1`.
- Verified neural garment similarity with Transformers CLIP: `make vton-clip-similarity-sample` runs `openai/clip-vit-base-patch32` on CPU, records image-image similarity plus text-prompt scores, and the evaluation index surfaces the report as `vton_clip`.
- Added native Rust semantic-cache admission at the gateway: LLM prompts are admitted or skipped before FastAPI, sensitive prompts are kept out of cache admission, `x-tryops-edge-cache-*` headers are forwarded, and `make native-edge-cache-smoke` verifies Prometheus metrics for both admitted and sensitive-skip decisions.
- Extended the Rust gateway semantic-cache path to invoke the native C++ lookup CLI at the edge when configured, emit `x-tryops-edge-cache-lookup-*` headers, and export `tryops_gateway_semantic_cache_lookups_total`; `make native-edge-cache-smoke` now proves a C++ cache hit plus the sensitive-prompt skip path without Python.
- Added native C++ GGUF CPU-first preflight split across header/source/CLI/test modules, downloaded a real `SmolLM2-135M-Instruct-Q2_K.gguf` artifact, parsed GGUF v3 metadata/tensor headers without Python, surfaced `tryops.native_gguf_preflight.v1` in the evaluation index as `llm_gguf`, and marked E010 complete with live llama.cpp generation explicitly pending `llama-cli`.
- Added native Go vLLM serving probe split across config, environment, HTTP, probe, JSON, latency, report, main, and test modules. `make llm-vllm-probe-sample` emits `tryops.vllm_serving_probe.v1`, detects the NVIDIA L4, checks `/v1/models`, `/v1/chat/completions`, and `/metrics`, and is surfaced in the evaluation index as `llm_vllm`; current evidence is skipped because `vllm` is not installed and no local endpoint is serving.
- Added native Go GPTQ/AWQ model preflight split across config, runtime, Hugging Face HTTP, probe, JSON, report, main, and test modules. `make llm-quantized-preflight-sample` verifies Qwen2.5-0.5B GPTQ-Int4 and AWQ configs plus SafeTensors artifact reachability, detects the NVIDIA L4 and local loader-package gaps, and surfaces `tryops.quantized_model_preflight.v1` in the evaluation index as `llm_quantized`.
- Added a native Go performance-budget gate split across config, typed artifact loading, budget evaluation, Markdown rendering, report, main, and tests. `make native-performance-budget-sample` consumes the Rust gateway benchmark, Go SLO gate, Go config contract, C++ perf stats, and native binary artifacts, then emits `tryops.native_performance_budget.v1` plus a CI-ready Markdown summary.
- Wired C++ VTON preprocessing and image metrics into the API execution path: `/v1/vton/infer` now returns a `tryops.native_vton_execution.v1` block, sidecar reports persist `native_execution`, request rows store native quality for dashboard/request details, and `make vton-native-api-sample` proves C++ preprocessing, C++ image metrics, and DB quality persistence through the API contract.
- Added the shared native trace/log envelope across Rust, Go, C++, and FastAPI: the contract is documented in `contracts/native_trace_log_envelope.schema.json`, FastAPI structured logs now embed `tryops.native_trace_log_envelope.v1`, the Rust gateway has a typed envelope module, C++ provides a validator CLI/library, and the split Go validator emits `tryops.native_trace_envelope.v1` with 4/4 runtime coverage via `make native-trace-envelope-sample`.
- Added the PA084 split-container contract: separate Dockerfiles now cover controller, benchmark, C++ tool suite, and standalone web assets in addition to API/gateway/guardrail; Compose optional profiles wire controller/tooling/assets services; and `native/go/tryops-container-contract/` emits `tryops.native_container_contract.v1` with 7/7 image roles and 87/87 checks passing.
- Added the PA086 native quota read model: `native/go/tryops-quota-read-model/` transforms Rust quota snapshots into tenant utilization/showback evidence, `/api/quota/summary` serves `tryops.native_quota_read_model.v1` through the BFF, and the Console Dashboard renders tenant risk, used units, native-source status, and showback.
- Added native Go runtime telemetry: `native/go/tryops-runtime-telemetry/` exports LLM benchmark/Pareto tokens/sec, variant peak VRAM, and live `nvidia-smi` GPU memory/utilization/power into `tryops.native_runtime_telemetry.v1` plus Prometheus text, closing H007/H008 with compiled evidence.
- Added native observability contract evidence: `infra/otel/collector.yml` wires OTLP/filelog Collector pipelines, Compose runs `otel-collector`, Prometheus scrapes it, the Rust gateway emits native JSONL trace-log envelopes, and `native/go/tryops-observability-contract/` validates gateway/API trace correlation in `tryops.native_observability_contract.v1`.
- Closed PA044 local secret-loading evidence: `.env.example` documents required secret variables, Compose mounts Postgres/MinIO/MLflow/gateway quota credentials through secrets, the Rust gateway reads `TRYOPS_GATEWAY_QUOTA_POSTGRES_DSN_FILE`, and the native Go config contract now fails on direct credential env regressions.
- Closed PA058 native Postgres migration/pooling evidence: `infra/postgres/migrations/` defines product and quota schema DDL, `native/go/tryops-db-migrator/` applies migrations through pgxpool, plan mode passes 20/20 checks, and live apply against Compose Postgres passes 33/33 checks with idempotent re-run evidence.
- Closed PA059 native backup/restore evidence: `native/go/tryops-backup-restore/` validates Compose storage and `infra/backup/restore_drill.cron`, then live mode uses the Postgres container's matching PG16 `pg_dump`/`pg_restore` tools plus MinIO `mc mirror`; the live drill passed 50/50 checks, restored seven Postgres tables into `tryops_restore_drill`, mirrored one object into `tryops-restore-drill`, and cleaned temporary restore targets.
- Added native Alertmanager routing evidence: `infra/alertmanager/alertmanager.yml` defines page/ticket receivers, inhibition, grouping, and a controller webhook target; Prometheus forwards alerts to `alertmanager:9093`; Compose runs Alertmanager; the Go controller accepts `/alerts/webhook`; and `native/go/tryops-alertmanager-contract/` validates 16 alert rules plus routing in `tryops.native_alertmanager_contract.v1`.
- Closed PA061 native TLS termination evidence: the Rust gateway now serves HTTPS through axum-server/rustls when cert/key env vars are set, Compose has an optional `gateway-tls` profile with `tryops_tls_cert` and `tryops_tls_key` secrets, `native/go/tryops-tls-contract/` validates plan/live coverage, and `make native-tls-smoke` passed with TLS1.3, HTTPS health 200, and plaintext HTTP rejection.
- Closed PA055 RBAC enforcement: `configs/api_keys.json` now defines viewer/operator/admin principals, FastAPI exposes a `tryops.rbac_session.v1` session route, the Rust gateway preflights `/v1/auth/session` with `session:read`, and the React Console filters navigation from the active session permissions while using the active key for promotion drills.
- Added PA064 native Go full-stack load/SLO evidence: `native/go/tryops-fullstack-load/` starts the FastAPI BFF and Rust gateway, drives six product scenarios through `/api/*`, emits `tryops.native_fullstack_load.v1`, and is indexed by the evaluation registry; the latest run passed 504 requests with zero errors, while k6/locust external confirmation remains pending because neither binary is installed locally.
- Added PA062 native CI/supply-chain contract evidence: `.github/workflows/ci.yml` now defines the Python/Node/Go/Rust/C++ test path, Compose validation, seven-image Docker Buildx matrix, Syft SBOM generation, Trivy HIGH/CRITICAL scan gate, artifact uploads, and Cosign keyless signing; `native/go/tryops-ci-contract/` emits `tryops.native_ci_contract.v1` with 16/16 checks passing and explicit missing local Syft/Trivy/Cosign tools.
